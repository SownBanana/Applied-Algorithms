#include "stdio.h"
#include "math.h"
int main(){
    unsigned long a, b;
    scanf("%d%d", &a, &b);
   // int s = (unsigned long)pow(a,b)%(unsigned long)(pow(10,9)+7);
    printf("%d", s);
    return 0;
}
